
# script to extract needed values (data vs SRA IDs_ from metadata json (table: sra_accessions)
while(<STDIN>) {
	chomp;
	$flag = 1 if(/\"metadata\":/);
	if($flag) {
		print "$1\t" if(/([DES]R[ASPR]\d+)/);
		print "$1\t" if(/\"(\d{4}+\-\d\d\-\d\d)/);
	}
	if(/\}/) {
		print "\n";
		$flag = 0;
	}
}
